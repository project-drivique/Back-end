spring:
  datasource:
    url: jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:drivique_dev}
    username: ${DB_USERNAME:drivique}
    password: ${DB_PASSWORD:drivique}
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true

logging:
  level:
    com.drivique: DEBUG
